# AI Story Video Factory

Automatically generates 3-5 minute moral/kids stories using Gemini AI, creates voiceover, builds video with FFmpeg, and uploads to YouTube.

## Setup
Add these secrets in GitHub → Settings → Secrets and variables → Actions:
- GEMINI_API_KEY
- CLIENT_ID
- CLIENT_SECRET
- REFRESH_TOKEN

Run manually from Actions tab or it runs daily.